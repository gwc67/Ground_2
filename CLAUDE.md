## Agent skills

### Issue tracker

Issues 以本地 Markdown 文件形式存放在 `.scratch/<feature>/` 目录下。见 `docs/agents/issue-tracker.md`。

### Triage labels

五个标准分类角色，标签字符串使用默认值。见 `docs/agents/triage-labels.md`。

### Domain docs

单上下文布局 — 根目录一个 `CONTEXT.md` + `docs/adr/`。见 `docs/agents/domain.md`。
